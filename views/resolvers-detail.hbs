{{> above}}

<div class="container">
{{this.description}}
</div>

<table class="table table-striped">
    <tbody>
        <tr>
            <td>Name</td>
            <td>{{resolver.name}}</td>
        </tr>
        {{#if resolver.tags}}
        <tr>
            <td>Tags</td>
            <td>
                {{#each resolver.tags}}
                <!--<a class="badge badge-secondary p-1" href="{{this}}">{{this}}</a>-->
                <span class="badge badge-secondary p-1">{{this}}</span>
                {{/each}}
            </td>
        </tr>
        {{/if}}
        <tr>
            <td>Website</td>
            <td><a href="{{resolver.url}}">{{resolver.url}}</a></td>
        </tr>
        {{#if resolver.social}}
        <tr>
            <td>Social media</td>
            <td>
            {{#each resolver.social}}
                <a class="pr-2" href="{{this}}">{{@key}}</a>
            {{/each}}
            </td>
        </tr>
        {{/if}}
        <tr>
            <td>Company</td>
            <td>{{resolver.company}}</td>
        </tr>
        <tr>
            <td>Location</td>
            <td>
            {{#if resolver.location}}
            <img alt="{{resolver.location}}" src="/images/flags/{{toUpper resolver.location}}.png" style="height:20pt;" title="{{resolver.location}}" />
            {{else}}
            <i>(unknown)</i>
            {{/if}}
            </td>
        </tr>
        <tr>
            <td>IP v4</td>
            <td>{{arrayToHtml resolver.ipv4}}</td>
        </tr>
        {{#if resolver.ipv6}}
        <tr>
            <td>IP v6</td>
            <td>{{arrayToHtml resolver.ipv6}}</td>
        </tr>
        {{/if}}
    </tbody>
</table>

<h2>Test {{resolver.name}}</h2>

<div class="container d-flex flex-column">
    <div class="row align-items-center justify-content-center no-gutters min-vh-100">
        <div class="col-12 col-md-5 col-lg-4 py-8 py-md-11">
            <form action="index.html" class="mb-6" method="POST" role="form">

                <div class="form-group">
                    <label for="hostname">
                        Host name
                    </label>
                    <input type="text" class="form-control" id="hostname" name="hostname" placeholder=""
                        value="{{hostname}}" />
                </div>
                <button class="btn btn-block btn-primary" type="submit">
                    Check
                </button>

            </form>
        </div>
    </div> <!-- / .row -->
</div> <!-- / .container -->


{{> below}}
